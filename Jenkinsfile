pipeline{
    agent {label 'jenkins-slave-shreyas'}
    stages{
        stage('Build'){
            steps{
                echo "Build"
            }
        }

        stage('Test'){
            steps{
                echo "Test"
            }
        }
    }
}
