pipeline {
    agent {label 'jenkins-slave-ganesh'}
    stages{
        stage('Build'){
            steps{
                echo "Build stage"
            }
        }
        stage('Test'){
            steps{
                echo "Test stage"
            }
        }
    }
}
